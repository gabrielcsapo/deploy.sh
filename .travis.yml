language: node_js
addons:
  apt:
    sources:
          - ubuntu-toolchain-r-test
        packages:
          - libstdc++-4.9-dev
before_install: npm install npm@3 -g
node_js:
  - "4"
services:
  - redis-server
